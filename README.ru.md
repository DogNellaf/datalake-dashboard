# DataLake

> [🇬🇧 English](README.md) | 🇷🇺 Русский

Django-приложение для загрузки, обработки и визуализации табличных данных о продажах. Файлы проходят через четырёхуровневый конвейер обработки и предоставляются через интерактивный конструктор графиков и интерфейс предварительного просмотра данных.

## Возможности

- Управление пользователями — регистрация и вход с правами доступа уровня staff
- Загрузка CSV — настраиваемый разделитель столбцов, автоматическое определение кодировки (UTF-8 / CP-1251)
- Четырёхуровневый ETL-конвейер: Raw (оригинальный файл), Ingest (CSV в UTF-8), Table (Parquet с автоопределением типов), Distilled (Parquet без дублей)
- Проверки качества данных — валидация схемы, обнаружение дубликатов и пропусков, поиск специальных символов, проверка числового преобразования
- Интерактивный конструктор графиков — выбор столбцов перетаскиванием, типы: точечный / линейный / столбчатый (Plotly)
- Предварительный просмотр данных — первые 100 строк в виде HTML-таблицы
- Статистика по столбцам — количество значений, пропуски, уникальные значения, мин / макс / среднее / стандартное отклонение для числовых столбцов
- Экспорт CSV — потоковая выгрузка обработанного набора данных
- Интеграция с Django admin — загрузка датасетов и переход к графику / просмотру / экспорту в один клик

## Технологический стек

| Уровень | Технология |
|---|---|
| Веб-фреймворк | Django 4.2 |
| Обработка данных | pandas, pyarrow |
| Визуализация | Plotly.js |
| Интерфейс | Bootstrap 5, jQuery UI |
| База данных | SQLite (для разработки) |

## Требования

- Python 3.11+

## Установка

```bash
# Клонировать репозиторий
git clone <url-репозитория>
cd app

# Создать и активировать виртуальное окружение
python -m venv .venv
source .venv/bin/activate      # Linux / macOS
.venv\Scripts\activate         # Windows

# Установить зависимости
pip install -r requirements.txt

# Применить миграции базы данных
python manage.py migrate

# Запустить сервер разработки
python manage.py runserver
```

Приложение будет доступно по адресу `http://127.0.0.1:8000/`.

## Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|---|---|---|
| `DJANGO_SECRET_KEY` | Секретный ключ Django — обязательно задать в продакшене | Небезопасный ключ |
| `DJANGO_DEBUG` | Режим отладки (`True`/`False`) | `true` |
| `DJANGO_ALLOWED_HOSTS` | Список разрешённых хостов через запятую | `*` (при DEBUG=true) |

## Запуск тестов

```bash
python manage.py test main.tests
```

## Структура проекта

```
app/
├── app/                  # Настройки Django и маршрутизация URL
│   ├── settings.py
│   └── urls.py
├── main/                 # Основное приложение
│   ├── models.py         # Модель данных и ETL-конвейер
│   ├── views.py          # Представления и API-эндпоинты
│   ├── forms.py          # Формы аутентификации
│   ├── admin.py          # Конфигурация панели администратора
│   ├── utils.py          # DataQualityTester, ErrorsAlerting
│   ├── tests.py          # Набор тестов (80 тестов)
│   ├── storage/
│   │   └── dfs.py        # Эталонная реализация для PySpark/Delta Lake
│   └── templates/
│       ├── base.html
│       ├── login.html
│       ├── register.html
│       ├── graph.html
│       └── preview.html
├── media/                # Уровни хранилища данных (создаются автоматически)
│   ├── raw/
│   ├── ingest/
│   ├── table/
│   └── destilled/
└── requirements.txt
```

## Лицензия

[MIT](LICENSE)
